<h1> My Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li class="article-list"><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>

<%= link_to 'Add a new article', new_article_path %>
